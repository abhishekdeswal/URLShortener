A simple URL Shortener
Made for localhost

To use copy the three(.htaccess and the php files) files in the server directory (C:\xampp\htdocs\ in my case)

To shorten a url go to localhost/url_short.php

